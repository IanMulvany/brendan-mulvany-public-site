# Python

*.shm
*.wal

__pycache__/
**/__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
# dist/ and public/ - We need public/ folder for Vercel static site
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
env/
ENV/
.venv

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Databases (exclude working databases, keep demo)
public_site.db
*.db
!demo.db
turso_public_db_info.yaml
turso_tokens.txt 

# local test script 
run_with_turso.sh
*memory*

# Local configuration files (keep examples)
config.yaml
config.local.yaml
*.local.yaml

# Environment files
.env
.env.local
.env.*.local

# Logs
*.log
logs/

# Beads local state
.beads/

# Work in progress
/newsite/

# Storage directories
storage/
storage-test/
public_storage_mock_dir/
public/
# we are ignoring public as we are now deploying to vercel via `vercel --deploy` 

# Temp files
*.tmp
.cache/

# Archive folder (contains old scripts with secrets)
archive/

# OS
Thumbs.db
.vercel
uv.lock
*.sql
*.shm
*.wal
meta_tags.json
*.lock
public_site.db-shm
public_site.db-wal

# Cloudflare preview: track reproducible schema and npm lockfile
!cloudflare-preview/migrations/*.sql
!cloudflare-preview/package-lock.json
